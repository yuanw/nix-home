# -*- mode: snippet -*-
# name: interface
# key: interface
# uuid: interface
# --
interface ${1:`(file-name-base (or (buffer-file-name)
                                   (buffer-name)))`} {
          $0
}